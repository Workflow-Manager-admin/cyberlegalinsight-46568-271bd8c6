import React from 'react';

// PUBLIC_INTERFACE
/**
 * ThemeToggle - Placeholder for theme toggle (light/dark mode).
 * This component will allow switching between light and dark themes.
 */
function ThemeToggle() {
  return (
    <div style={{margin: "0 0 18px 0"}}>
      <button className="btn" style={{fontSize:".96rem", background:"var(--secondary)"}} disabled>[Stub] Toggle Theme</button>
    </div>
  );
}

export default ThemeToggle;
